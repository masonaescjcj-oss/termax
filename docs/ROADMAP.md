# نقشه‌راه ترمکس

نقشه‌راه کامل تبدیل ترمکس به یک اپ حرفه‌ای AI ترید — از وضعیت امروز تا محصول
قابل عرضه. هر فاز خروجی مشخص، معیار پذیرش و وابستگی دارد، تا هر جلسه‌ی کاری
بدانیم دقیقاً کجاییم.

زیرساخت تصمیم‌گیری‌شده: **یک سرور کوچک (۴ هسته / ۸ گیگ کافی است) + Supabase Pro**.
با Pro سقف دیتابیس ۸ گیگابایت و egress بسیار بازتر است، اما دو قاعده‌ی معماری
سر جایشان می‌مانند چون مسئله‌شان تأخیر است نه سهمیه: کندل‌ها روی دیسک می‌مانند
نه در دیتابیس، و حلقه‌ی تیک از حافظه می‌خواند نه از Supabase.

آخرین به‌روزرسانی: ۲۲ آگوست ۲۰۲۶

---

## وضعیت امروز — چه داریم

| بخش | وضعیت |
|---|---|
| موتور محاسبات (فارکس درست، bid/ask، تبدیل ارز، سواپ، مارجین) | ✅ ساخته و تست‌شده |
| فید چندمنبعی (cTrader فارکس/فلزات/شاخص، Binance کریپتو، Yahoo پشتیبان) | ✅ ساخته شده — تست زنده مانده |
| دو حالت معاملاتی: شبیه‌سازی + اجرای واقعی روی cTrader | ✅ ساخته شده |
| OAuth امن cTrader (state امضاشده، ذخیره و تازه‌سازی توکن) | ✅ |
| اپ موبایل/وب (چارت، واچ‌لیست، پوزیشن‌ها، چت، MaxAI) | ✅ موجود و اجراشده |
| تست‌ها | ✅ ۵۱۸ اَسِرشن، همه سبز |
| طرح معماری AI | ✅ `docs/ai-architecture.md` |

---

## تصمیم‌های قفل‌شده

این‌ها بحث شده و بسته شده‌اند؛ دوباره بازشان نمی‌کنیم مگر شواهد جدید:

۱. **ربات = داده، نه برنامه.** مشخصات JSON اعلانی + یک مفسر مشترک. هزار ربات ≈ ۹
   مگابایت.
۲. **اندیکاتور یک بار محاسبه، در باس مشترک.** کلید: (نماد، تایم‌فریم، پارامترها).
۳. **بک‌تست = همان مفسر روی ساعت کندلی، در `worker_threads`.** هرگز روی thread
   اصلی.
۴. **ربات بدون فوروارد تست کامل، لایو نمی‌شود.** بدون استثنا.
۵. **مدل AI هیچ عددی را محاسبه نمی‌کند** — فقط خروجی ابزار را گزارش می‌کند و
   عددها ساخت‌یافته رندر می‌شوند.
۶. **کد سفارشی فقط QuickJS روی WASM، استخری، در پلن پولی.** نه `isolated-vm`.
۷. **کندل روی دیسک باینری؛ وضعیت زنده در حافظه؛ Supabase برای دوام.**
۸. **سهمیه‌ی AI از روز اول.** هزینه‌ی توکن > هزینه‌ی سرور.

---

## فازها

ترتیب یعنی وابستگی. تخمین‌ها «روز کاری متمرکز» هستند و ± یک روز خطا دارند.

### فاز ۰ — استقرار پایه · ۲ تا ۳ روز · با هم

سرور و Supabase Pro را به مدار می‌آوریم تا بقیه‌ی فازها جای واقعی برای اجرا
داشته باشند.

- [ ] چرخاندن اعتبارنامه‌های لو رفته (کلید AI، MongoDB، توکن تلگرام) — **شما**
- [ ] ثبت اپ در `openapi.ctrader.com/apps` + گرفتن توکن Playground — **شما**
- [ ] اجرای `migrations/001_add_venue_columns.sql` روی Supabase Pro — **شما یا من**
- [ ] Caddyfile لینوکسی + سرویس systemd + استقرار بک‌اند و بیلد وب — **من**
- [ ] پر کردن `.env` روی سرور و تست زنده‌ی فید cTrader (چک‌لیست ۶ مرحله‌ای در `REMAINING.md`) — **با هم**

**معیار پذیرش:** `/api/health` از دامنه جواب بدهد؛ لاگ `Matched N instruments`
دیده شود؛ اسپرد EUR/USD واقعی باشد نه synthetic.

### فاز ۱ — موتور استراتژی · ۳ تا ۴ روز · **شروع شد ✅**

گرامر مشخصات ربات، اعتبارسنج سخت‌گیر، اندیکاتورهای افزایشی و مفسر — قلب همه‌ی
فازهای بعدی. بدون نیاز به فید، با کندل مصنوعی تست می‌شود.

- [x] انواع و گرامر v1 (`services/strategy/types.ts`)
- [x] اعتبارسنج با پیام خطای مسیر‌دار برای حلقه‌ی AI (`validate.ts`)
- [x] اندیکاتورهای افزایشی: SMA، EMA، RSI، ATR، MACD، Bollinger، Stochastic، Highest/Lowest (`indicators.ts`)
- [x] سری کندل با `Float64Array` + تجمیع‌گر تایم‌فریم (`series.ts`)
- [x] مفسر: فیلتر سشن/زمان/اسپرد، ورود، خروج سیگنالی، timeStop، سقف معامله‌ی روزانه، cooldown، بدون نگاه به آینده (`interpreter.ts`)
- [x] تست در برابر پیاده‌سازی مرجع مستقل

**معیار پذیرش:** تست‌ها سبز؛ یک استراتژی EMA-cross روی داده‌ی مصنوعی دقیقاً در
کندل تلاقی سیگنال بدهد، نه قبل نه بعد.

### فاز ۲ — سرویس ربات زنده (paper) · ۴ تا ۶ روز · **بک‌اند ساخته شد ✅**

مفسر به فید وصل می‌شود و ربات‌ها روی حساب شبیه‌سازی معامله می‌کنند.

- [x] ذخیره‌ی کندل باینری روی دیسک (یک فایل / نماد / ماه) — `services/candles/store.ts`
- [ ] backfill کندل تاریخی از فیدها (پیش‌نیاز بک‌تست؛ در فاز ۳ انجام می‌شود)
- [x] تجمیع‌گر زنده: تیک ← کندل‌های همه‌ی تایم‌فریم‌ها — `services/candles/liveBars.ts`
- [x] BotRunner: بارگذاری مشخصات از دیتابیس، ایندکس نماد، ارزیابی در بسته‌شدن کندل، اجرا از همان هسته‌ی سفارش شبیه‌سازی که API انسانی استفاده می‌کند — `services/bots/runner.ts`
- [x] صف سفارش با انصاف به‌ازای هر حساب
- [x] جدول `bots` در Supabase (مایگریشن ۰۰۲) + API ساخت/شروع/توقف/فهرست/حذف
- [x] صفحه‌ی «ربات‌های من» در اپ (`BotsScreen`): فهرست + آمار زنده + شروع/توقف/حذف؛ ورود از Tools Hub
- [x] batch کردن پخش قیمت سوکت (۶۰۰۰ پیام/ثانیه ← حداکثر ~۳ پیام/ثانیه/نماد)

**معیار پذیرش:** یک ربات نمونه ۲۴ ساعت روی دمو بدود، معامله باز و بسته کند،
و بعد از ری‌استارت سرور ادامه بدهد.

### فاز ۳ — بک‌تست + نمره‌ی صداقت · ۴ تا ۶ روز · **بک‌اند ساخته شد ✅**

- [x] اجرای مفسر روی کندل تاریخی در `worker_threads` (استخر ۲تایی + صف + سقف ۲ کار هم‌زمان هر کاربر + مهلت ۱۸۰ ثانیه) — `services/backtest/pool.ts`
- [x] هزینه‌های واقعی: اسپرد دوطرفه (بازسازی bid/ask از اسپرد معمول نماد)، کمیسیون هر لات، سواپ شبانه با ۳ برابر چهارشنبه، تبدیل ارز (جفت‌های دلاری دقیق از خود سری؛ کراس‌ها با نرخ ثابتِ اعلام‌شده در warnings) — `services/backtest/engine.ts`
- [x] SL/TP/تریلینگ در رزولوشن ۱ دقیقه؛ برخورد هم‌زمان SL و TP در یک کندل = استاپ (محافظه‌کارانه)
- [x] پنج بررسی نمره‌ی صداقت: out-of-sample، حجم نمونه، فشار پارامتر، حساسیت ±۱۰٪، مونت‌کارلوی ترتیب — `services/backtest/honesty.ts`
- [x] backfill کندل تاریخی از فیدها (klines بایننس + trendbars سی‌تریدر، صفحه‌بندی‌شده) — `services/candles/backfill.ts`
- [x] API: ساخت/فهرست/جزئیات/حذف بک‌تست (`/api/v1/backtests`) + جدول `backtests` (مایگریشن ۰۰۳)
- [x] نتیجه‌ی بک‌تست در اپ: آمار + نمره‌ی صداقت + پنج بررسی + هشدارها (داخل سازنده‌ی ربات)؛ نمودار equity و فهرست معاملات هنوز مانده
- [ ] کش نتیجه (همان مشخصات + همان بازه = همان جواب)

**معیار پذیرش:** بک‌تست ۳ ساله‌ی ۱۵ دقیقه زیر ۵ ثانیه؛ در حین اجرا هیچ وقفه‌ای
در فید قیمت نیفتد.

### فاز ۴ — فوروارد تست + دروازه‌ی لایو · ۳ تا ۴ روز · **بک‌اند ساخته شد ✅**

- [x] رکورد زنده‌ی فوروارد تست: `GET /api/v1/bots/:id/report` — معاملات، وین‌ریت، پروفیت‌فکتور، انتظار ریاضی، حداکثر افت، میانگین نگهداری + مقایسه با بک‌تست همان ربات («شکاف واقعیت»: نسبت انتظار فوروارد به انتظار بک‌تست)
- [x] قانون سخت لایو: حداقل روز + حداقل معامله (پیش‌فرض ۱۴ روز / ۲۰ معامله؛ با `LIVE_GATE_MIN_DAYS` و `LIVE_GATE_MIN_TRADES` قابل تنظیم) — `services/bots/liveGate.ts`؛ رکوردِ ضررده مسدود نمی‌شود ولی `acknowledgeLosingRecord: true` صریح می‌خواهد
- [x] «ربات در برابر تو»: آمار معاملات دستی کاربر در همان بازه، با همان فرمول‌ها، کنار آمار ربات
- [x] استقرار لایو با حداقل حجم به‌صورت پیش‌فرض: `POST /api/v1/bots/:id/go-live` — حالت `MIN` هر سفارش را با حداقل حجم نماد می‌فرستد؛ اعتماد به سایز خود اسپک فقط با انتخاب صریح `volumeMode: SPEC`
- [x] اجرای لایو در BotRunner از هسته‌ی سفارش لایوِ مشترک با API انسانی (`openLiveOrderCore` / `closeLivePositionCore` — مایگریشن ۰۰۴)
- [ ] تست یکپارچه روی حساب دموی واقعی cTrader (خارج از سندباکس — به‌محض توکن Playground)

### فاز ۵ — لایه‌ی ابزار AI · ۴ تا ۶ روز · **ساخته شد ✅**

- [x] ده ابزار تایپ‌شده (`services/ai/tools.ts`): `get_account`، `get_positions`، `get_trade_stats`، `get_quote`، `get_candles`، `get_indicator`، `run_backtest`، `save_strategy`، `deploy_strategy`، `propose_order` — عمداً هیچ ابزار «اجرای سفارش» وجود ندارد؛ propose_order فقط کارت پیشنهاد با اعداد موتور می‌سازد و تأیید با انسان است
- [x] rollup آمار معاملات (`trade_stats_daily` — مایگریشن ۰۰۵): آپدیت در لحظه‌ی بسته‌شدن (هر دو مسیر شبیه‌سازی و لایو)؛ «این ماه چطور بودم؟» = ۳۰ ردیف کوچک، نه جمع کل تاریخ
- [x] کلاینت: کارت `order_proposal` با اعداد موتور (ریسک دلاری، R:R، مارجین) و دکمه‌ی تأییدِ همان حجم پیشنهاد — دکمه‌ی قدیمی «Execute 1.00 Lot» و نشان `confidence` حذف شد؛ سرور هم هر confidence باقی‌مانده را strip می‌کند
- [x] پرامپت سیستم استاتیک (کش‌شونده — عدد زنده داخلش نیست، از ابزار می‌آید) + مسیر ارزان برای احوال‌پرسی + سقف ۴ دور ابزار در هر نوبت
- [x] سهمیه: پیش‌فرض ۳۰ پیام/روز (`AI_FREE_DAILY_MSGS`)، جدول `ai_usage`، پاسخ ۴۲۹ با payload پی‌وال، endpoint شمارنده `GET /api/v1/ai/usage`
- [ ] پی‌وال UI کامل در اپ (فعلاً پیام سهمیه نمایش داده می‌شود)

### فاز ۶ — ساخت ربات با زبان طبیعی · ۳ تا ۵ روز · **بک‌اند ساخته شد ✅ — MVP اینجا کامل است**

- [x] حلقه‌ی نویسندگی (`services/ai/botBuilder.ts` + `POST /api/v1/bots/build`): توصیف فارسی/انگلیسی ← JSON محدود به schema (گرامر کامل در پرامپت استاتیک) ← اعتبارسنجی با retry تا ۳ نوبت (خطاهای مسیردار عیناً به مدل برمی‌گردد) ← بک‌تست خودکار با نمره‌ی صداقت ← خروجی: اسپک + قواعد + نتیجه؛ هر build = ۱ پیام از سهمیه
- [x] رندر مشخصات به جمله‌ی فارسی و انگلیسی (`services/strategy/describe.ts`) — **قطعی و از خودِ JSON نهایی**، نه بازگویی مدل («ورود خرید وقتی: RSI(14) از 30 رو به بالا عبور کند و قیمت پایانی بالاتر از EMA(200) @4h باشد.»)؛ همین قواعد در `GET /bots/:id/report` هم می‌آید
- [x] صفحه‌ی سازنده‌ی ربات در اپ: توصیف ← قواعد فارسی + بک‌تست + نمره؛ تنها دکمه‌ی اقدام: «Start forward test»

### فاز ۷ — چارت یکپارچه · ۴ تا ۷ روز

- [ ] مارکر ورود/خروج ربات و معاملات بک‌تست روی چارت موجود
- [ ] رسم اندیکاتورهای مشخصات روی چارت (پنل جدا/روی قیمت)
- [ ] پخش زنده‌ی وضعیت ربات روی چارت (خط SL/TP، ناحیه‌ی فعال)

### فاز ۸ — Trade DNA و هشدار هوشمند · ۳ تا ۵ روز

- [ ] پروفایل رفتاری از معاملات بسته: revenge trading، بستن زود سود، بدترین ساعت/روز، خزش حجم
- [ ] «چرا این ضرر داد؟» — کالبدشکافی یک‌تپی معامله با کندل‌های اطراف ورود/خروج
- [ ] هشدارهایی که دلیل می‌گویند (وصل به مشخصات ربات‌های کاربر)

### فاز ۹ — اندیکاتور سفارشی + لایه‌ی کد · ۵ تا ۸ روز

- [ ] گرامر عبارت اندیکاتور + ثبت در همان باس + رسم روی چارت
- [ ] استخر QuickJS/WASM (۸ نمونه، بودجه‌ی گام، timeout، جبرگرایی اجباری) — پلن پولی

### فاز ۱۰ — جامعه و درآمد · ۷ تا ۱۰ روز

- [ ] کتابخانه‌ی استراتژی منتشرشده با **رکورد فوروارد تست** (نه بک‌تست) + لیدربرد
- [ ] حالت بازپخش (تمرین کندل‌به‌کندل روی تاریخ، در برابر ربات)
- [ ] پلن‌ها: رایگان (سهمیه‌ی AI محدود، ربات محدود) / پرو (ربات بیشتر، کد سفارشی، AI بیشتر)

---

## قابلیت‌های تکمیلی (بعد از MVP، به ترتیب ارزش/هزینه)

- **ژورنال خودکار** — هر معامله با زمینه‌ی بازار و برچسب AI ثبت شود؛ خوراک Trade DNA
- **گزارش هفتگی خودکار** — پوش/ایمیل: عملکرد هفته، بدترین عادت، پیشنهاد يک اقدام
- **نگهبان ریسک** — سقف ضرر روزانه که کاربر خودش تنظیم می‌کند و AI اجرایش می‌کند (قفل معامله تا فردا)
- **اسکنر ستاپ** — مشخصات یک ربات را روی همه‌ی نمادها بدود و ستاپ‌های فعال را فهرست کند
- **حالت آموزش** — بازپخش + توضیح AI روی هر تصمیم

---

## بودجه‌ی ماهانه

| قلم | هزینه | وضعیت |
|---|---|---|
| سرور کوچک | ~۲۰ دلار | دارید ✅ |
| Supabase Pro | ۲۵ دلار | دارید ✅ |
| دامنه | ~۱ دلار | — |
| cTrader Open API / Binance | صفر | — |
| توکن AI | متغیر — **با سهمیه مهار می‌شود** | فاز ۵ |

با سهمیه‌ی پلن رایگان و مدل دولایه، هزینه‌ی AI تقریباً خطی با کاربر پولی رشد
می‌کند نه با کل کاربران — این تنها مدل پایدار روی این بودجه است.

---

## دروازه‌های ریسک (قبل از عبور از هر کدام، توقف)

۱. **قبل از فاز ۰ تمام‌شده:** اعتبارنامه‌های لو رفته چرخانده شده باشند.
۲. **قبل از هر سفارش لایو:** چک‌لیست ۶ مرحله‌ای `REMAINING.md` روی دمو کامل شده باشد؛ اولین سفارش لایو ۰.۰۱ لات.
۳. **قبل از عرضه‌ی عمومی سیگنال/ربات:** سلب مسئولیت + حذف هر ادعای عملکرد + بررسی حوزه‌ی قضایی کاربران.
۴. **قبل از فعال کردن لایه‌ی کد (فاز ۹):** بازبینی امنیتی مرز WASM.

---

## همین هفته

**شما:**
- [ ] چرخاندن سه اعتبارنامه‌ی لو رفته
- [ ] ثبت اپ cTrader + توکن Playground از حساب دمو
- [ ] دسترسی سرور + دامنه (اگر دارید)

**من (شروع شده):**
- [x] فاز ۱: گرامر + اعتبارسنج + اندیکاتورها + مفسر + تست‌ها
- [x] فاز ۰: فایل‌های استقرار (`deploy/Caddyfile`، `deploy/termax-backend.service`، `deploy/deploy.sh`)
- [x] فاز ۲ (بک‌اند): ذخیره‌ی کندل، کندل‌ساز زنده، BotRunner، API ربات‌ها
- [x] فاز ۳ (بک‌اند): موتور بک‌تست، نمره‌ی صداقت، backfill، استخر worker، API
- [x] فاز ۴ (بک‌اند): گزارش فوروارد تست، دروازه‌ی لایو، go-live با حداقل حجم
- [x] فاز ۵: لایه‌ی ابزار AI، سهمیه، rollup آمار، حلقه‌ی ابزار MaxAI
- [x] فاز ۶ (بک‌اند): سازنده‌ی ربات با زبان طبیعی + رندر فارسی قواعد — ۵۱۸ اَسِرشن سبز
- [x] بسته‌ی UI ربات‌ها: «ربات‌های من» + سازنده + گزارش (کارنامه، شکاف واقعیت، ربات در برابر تو، دروازه‌ی لایو + go-live)
- [ ] فاز ۷: چارت یکپارچه (قدم بعدی)
